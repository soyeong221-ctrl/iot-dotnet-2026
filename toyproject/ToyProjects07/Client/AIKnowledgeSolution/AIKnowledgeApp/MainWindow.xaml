<Window x:Class="AIKnowledgeApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AIKnowledgeApp"
        mc:Ignorable="d" Topmost="True"
        Title="MainWindow" Height="650" Width="900">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 타이틀 -->
        <TextBlock Grid.Row="0" 
                   Text="AI 문서 지식검색 시스템"
                   FontSize="25" FontWeight="Bold" 
                   Margin="0,0,0,20" FontFamily="Noto Sans KR Medium"/>

        <!-- PDF 선택 및 등록 -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="110" />
            </Grid.ColumnDefinitions>

            <TextBox x:Name="TxtPdfPath" Grid.Column="0" Height="38" VerticalContentAlignment="Center"
                     IsReadOnly="True" Margin="0,0,10,0" FontFamily="Noto Sans KR Medium" />
            <Button x:Name="BtnSelPdf" Grid.Column="1" Content="PDF 선택" Height="38" Margin="0,0,10,0" Click="BtnSelPdf_Click" />
            <Button x:Name="BtnUpload" Grid.Column="2" Content="문서 등록" Height="38" Click="BtnUpload_Click" />
        </Grid>

        <!-- 질문 영역 -->
        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="120" />
            </Grid.ColumnDefinitions>

            <TextBox x:Name="TxtQuestion" Grid.Column="0" Height="45" VerticalContentAlignment="Center"
                     Margin="0,0,10,0" FontFamily="Noto Sans KR Medium" />
            <Button x:Name="BtnQuestion" Grid.Column="1" Content="질문하기" Height="45" Click="BtnQuestion_Click" />
        </Grid>

        <!-- AI 답변 영역 -->
        <GroupBox Grid.Row="3" Header="AI 답변" FontSize="16">
            <TextBox x:Name="TxtAnser"
                     Margin="10" FontSize="14" TextWrapping="Wrap"
                     AcceptsReturn="True" 
                     VerticalScrollBarVisibility="Auto"
                     IsReadOnly="True"/>
        </GroupBox>
    </Grid>
</Window>
