<Page x:Class="WpfBasic02Navi.Sub04Page"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfBasic02Navi"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="전통 윈폼 바인딩" Loaded="Page_Loaded" >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="5*" />
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <Label Content="전통 윈폼 데이터바인딩" Grid.Row="0" Grid.Column="0" />
        <DataGrid x:Name="DgrEmployees" Grid.Row="1" Grid.RowSpan="2" AutoGenerateColumns="True" Margin="5"
                  IsReadOnly="True" SelectionMode="Single">
        </DataGrid>

        <GroupBox x:Name="GrbInfo" Grid.Column="1" Grid.Row="1" Margin="5" Header="상세정보">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>

                <TextBox x:Name="TxtSpeed" Grid.Row="0" Margin="10" Text="" TextChanged="TxtSpeed_TextChanged" />

                <StackPanel Grid.Row="1" Margin="10,5">
                    <Border Height="20">
                        <Border.Background>
                            <SolidColorBrush x:Name="ScbColor" />
                        </Border.Background>
                    </Border>
                    <TextBox x:Name ="TxtColor" Text="" TextChanged="TxtColor_TextChanged" />
                </StackPanel>
                <TextBox Grid.Row="2" Margin="10" />
                <TextBox Grid.Row="3" Margin="10" />
                <TextBox Grid.Row="4" Margin="10" />
                <CheckBox Grid.Row="5" Margin="10" />
            </Grid>
        </GroupBox>

        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="신규" Margin="5,10" Width="70" />
            <Button Content="저장" Margin="5,10" Width="70" />
            <Button Content="삭제" Margin="5,10" Width="70" />
        </StackPanel>
    </Grid>

</Page>